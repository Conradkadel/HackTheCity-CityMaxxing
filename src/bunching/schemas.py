"""
Column names + dtypes of every contract table (used by tests + mocks).

OWNER:   A
INPUT:   CONTRACTS.md
OUTPUT:  schema dicts

TODO:
  [ ] one schema per table
  [ ] validate(df, table) function
"""
